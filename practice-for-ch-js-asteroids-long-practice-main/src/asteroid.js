import MovingObject from "./moving_object";

//Spacerock. It inherits from MovingObject.
class Asteriod extends MovingObject {
  constructor(options) {
    super(options);
  }
}

export default Asteriod;
